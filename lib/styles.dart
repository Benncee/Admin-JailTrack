import 'package:flutter/material.dart';

class AppStyles {
  static const Color sidebarColor = Color.fromARGB(255, 4, 14, 29);
  static const Color sidebarHoverColor = Colors.blue;
  static const TextStyle sidebarTextStyle = TextStyle(
    color: Colors.white,
    fontSize: 20,
    fontWeight: FontWeight.bold,
  );
  static const EdgeInsets sidebarItemPadding =
      EdgeInsets.symmetric(vertical: 8.0, horizontal: 16.0);
}
